import { MaskAccnaPipe } from './mask-accna.pipe';

describe('MaskAccnaPipe', () => {
  it('create an instance', () => {
    const pipe = new MaskAccnaPipe();
    expect(pipe).toBeTruthy();
  });
});
